        //var client = new HttpClient();
        //client.BaseAddress = new Uri("https://mobile.digistat.it/CandidateApi/");


        //string user = "test";
        //string password = "TestMePlease!";


        //// Combine the user and password and encode it in Base64
        //string authValue = Convert.ToBase64String(Encoding.ASCII.GetBytes($"{user}:{password}"));

        //// Add the Authorization header to the HttpClient request
        //client.DefaultRequestHeaders.Authorization = new AuthenticationHeaderValue("Basic", authValue);

        //try
        //{
        //    HttpResponseMessage response = await client.GetAsync("Patient/GetList");  // Modifica se necessario
        //    //Console.WriteLine($"URL chiamato: {client.BaseAddress}Patient/GetList");
        //    if (response.IsSuccessStatusCode)
        //    {
        //        string responseBody = await response.Content.ReadAsStringAsync();

        //        //Console.WriteLine(responseBody);

        //        // Se conosci il formato JSON della risposta, usa una classe specifica per la deserializzazione
        //        var patientsList = JsonSerializer.Deserialize<List<GetDataPatient>>(responseBody);



        //        //Console.WriteLine($"Il numero di pazzienti è -> {patientsList.Count}");
        //        if(patientsList != null && patientsList.Count > 0)
        //        {

        //            Console.WriteLine("Lista Pazzenti ottenuta");
        //            foreach ( var patient in patientsList)
        //            {

        //                Console.WriteLine($"ID: {patient.id}, Nome: {patient.givenName} {patient.familyName}, Data di nascita: {patient.birthDate}");
        //            }
        //        }
        //        else
        //        {
        //            Console.WriteLine("Non esistono pazienti");
        //        }
        //    }
        //    else
        //    {
        //        Console.WriteLine("Ci sono dei problemi 1)");
        //        Console.WriteLine($"Error: {response.StatusCode}");
        //    }
        //}
        //catch (Exception ex)
        //{
        //    Console.WriteLine("Ci sono dei problemi 2)");
        //    Console.WriteLine($"Exception: {ex.Message}");
        //}

        //app.MapGet("/", () => "Hello World!");